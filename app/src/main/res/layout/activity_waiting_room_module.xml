<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/waitingRoomModule"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tools:context=".WaitingRoomModule">

    <ImageView
        android:id="@+id/playerAvatar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/playerNameTextView"
        android:textColor="@color/white"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="@+id/playerAvatar"
        app:layout_constraintStart_toStartOf="@+id/playerAvatar"
        app:layout_constraintTop_toBottomOf="@+id/playerAvatar" />

    <TextView
        android:id="@+id/numberOfCardsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/round_corners_text_view"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:textStyle="bold"
        android:visibility="invisible"
        app:layout_constraintBottom_toTopOf="@+id/playerNameTextView"
        app:layout_constraintEnd_toEndOf="@+id/playerAvatar"
        app:layout_constraintStart_toEndOf="@+id/playerAvatar"
        app:layout_constraintTop_toTopOf="@+id/playerAvatar" />
</androidx.constraintlayout.widget.ConstraintLayout>